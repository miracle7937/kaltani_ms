import 'package:flutter/cupertino.dart';

class BailingScreen extends StatelessWidget {
  const BailingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
